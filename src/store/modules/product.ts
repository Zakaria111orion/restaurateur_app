import axios from "axios";
const state = {
    products: []
};
const getters = {
    allProducts:  (state: any) => state.products
};
const actions = {
   
};
const mutations = {

};

export default {
    state,
    getters,
    actions,
    mutations,
}